(() => {

    function callBatman(): void{
        //return 1 se queja porque tiene que devolver un void
    }

    const callSuperman = (): void => {
        return // esto devuelve un void
    }

    const a = callBatman();
    console.log(a);

    //no es lo mismo void, que undefined, que null
})()